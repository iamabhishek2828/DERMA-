// Google OAuth callback component has been disabled on the frontend.
// Frontend no longer exposes a Google auth UI flow. Backend endpoints may remain.

const GoogleAuthCallback = () => null;

export default GoogleAuthCallback;